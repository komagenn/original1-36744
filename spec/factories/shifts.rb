FactoryBot.define do
  factory :shift do
    work_id               {'2'}
    hour_id               {'2'}
    sum_id                {'2'}
    holiday               {'希望休なんてない'}
    remark                {'時給上げてくれーーーー'}
   user
  end
end
